[build]
builder = "nixpacks"
buildCommand = "npm ci && npm run build:shared && npm run build --workspace=letssplyt-backend"

[deploy]
startCommand = "node backend/dist/server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
