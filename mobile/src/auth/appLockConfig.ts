/** Idle time before app lock (memory session cleared; unlock required). */
export const APP_LOCK_IDLE_MS = 5 * 60 * 1000;

/** Failed biometric / device-credential attempts before forcing OTP re-login. */
export const BIOMETRIC_MAX_FAILURES = 3;
