# Backend URL for the mobile app (base URL only — no /api/v1 suffix).
# Optional in local dev: Expo Go auto-detects your Mac's LAN IP from the Metro bundler.
# Set manually if auto-detection fails or you use a remote backend:
# EXPO_PUBLIC_API_URL=http://192.168.1.42:3000
