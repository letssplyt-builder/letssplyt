import type { ReceiptItem } from '@letssplyt/shared/receipt.types';

/** Stub — full implementation in E07 stories. */
export async function storeReceiptItems(
  _eventId: string,
  _items: ReceiptItem[],
): Promise<void> {
  return;
}
